name: CI/CD Pipeline
on:
  workflow_dispatch:   # allows manual trigger
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow is valid and running!"
